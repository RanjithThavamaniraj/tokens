export default function Logo() {
  return (
    <span
      style={{
        fontFamily: "var(--font-heading)",
        fontSize: "1.125rem",
        letterSpacing: "0.12em",
        color: "var(--color-text)",
      }}
    >
      TOKENS
    </span>
  );
}
